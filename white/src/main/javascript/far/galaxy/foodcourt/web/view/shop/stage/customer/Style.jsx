export const ShopCustomersStyles = theme => ({ // todo, duplicated
    wrapped: {
        padding: 50,
    },
    paper: {
        padding: theme.spacing.unit * 2,
        textAlign: 'center',
        color: theme.palette.text.secondary,
        cursor: 'pointer'
    },
});
