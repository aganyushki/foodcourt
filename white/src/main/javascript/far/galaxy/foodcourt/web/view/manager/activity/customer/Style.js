
export const ManagerCustomersMainToolbarViewStyles = () => ({
    root: {
        display: 'flex'
    },
});

export const ManagerCustomersEditorStyles = theme => ({
    textField: {
        marginLeft: theme.spacing.unit,
        marginRight: theme.spacing.unit,
    },
    button: {
        margin: theme.spacing.unit,
    },
    editorContent: {
        padding: 50,
    },
});
